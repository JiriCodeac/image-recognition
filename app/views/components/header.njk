<!doctype html>
<html class="no-js" lang="en-US">
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Forester</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="https://kit.fontawesome.com/04a469eff1.css" crossorigin="anonymous">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.css"/>

    <meta http-equiv="x-dns-prefetch-control" content="on">
</head>
<body style="margin-top: 52px">
<script>
    window.deleteFrame = function (element) {
        const url = element.href;
        element.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>';
        element.disabled = true;
        fetch(url, {
            method: 'post',
        }).then(function (data) {
            console.log(data);
            if (data.ok) {
                element.parentElement.parentElement.parentElement.remove();
            } else {
                element.innerText = 'Failed';
            }
        });
        return false;
    }

    window.toggleSection = function (fileId) {
        const block = document.getElementById(`file-${fileId}`);

        if (block.style.display === "none") {
            block.style.display = "block";
        } else {
            block.style.display = "none";
        }
    }
</script>
<nav class="navbar navbar-expand fixed-top bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/#">
            <i class="fa-duotone fa-tree"></i>&nbsp;
            <strong>Forester</strong>
        </a>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" aria-current="page" href="/best-frames">
                    {{ __('best') | capitalize }}
                    <span class="d-none d-md-inline">{{ __('frames') | capitalize }}</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" aria-current="page" href="/latest-detections">
                    {{ __('latest') | capitalize }}
                    <span class="d-none d-md-inline">{{ __('frames') | capitalize }}</span>
                </a>
            </li>
        </ul>
        <span class="navbar-text">
            <i class="fa-duotone fa-user"></i>&nbsp;
            {{ userName }}
        </span>
    </div>
</nav>
<div class="container-fluid">
